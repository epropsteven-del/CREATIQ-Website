import { defineConfig } from "vite";

export default defineConfig({
  // Relative asset URLs work both on the repository Pages URL and at the
  // root of a custom subdomain, such as https://2ndu.creatiqai.my/.
  base: "./"
});
